Hand-written fixtures for endpoints that exist in the API's `feature/api-config-admin` branch (facility templates, capability catalogue,
rule definitions, operating rules) but are not on the shared node yet, so they cannot be captured. Shapes are taken from that branch's
code (`app/Domain/Config`); replace with recordings from `tests/Fixtures/real` once the node serves them.
