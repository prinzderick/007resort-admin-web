Hand-written fixtures for states the demo data cannot produce on demand (a CONFIGURATION sync conflict needs two diverged nodes).
Everything else under tests/Fixtures/real is recorded from a running node (php artisan r007:capture-fixtures).
